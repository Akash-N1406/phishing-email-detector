# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python
venv/
.venv/
env/
ENV/

# Jupyter
.ipynb_checkpoints/

# Data (raw/processed datasets are often large / not meant for git)
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Kaggle credentials - NEVER commit this
kaggle.json
.kaggle/

# Models: committed intentionally so the repo is clone-and-run for the
# Week 3 Django demo, without needing to retrain from scratch.
# (If repo size becomes a concern later, revisit this.)

# OS / editor
.DS_Store
.vscode/
*.swp

# Env vars
.env

# Django (webapp/)
webapp/db.sqlite3
webapp/staticfiles/
webapp/*.log